#!/bin/bash -e

# Migrations are intentionally NOT run here.
# In ECS, multiple tasks start concurrently — running db:migrate inside
# the entrypoint causes race conditions against RDS. Instead, run
# migrations as a one-off ECS task in your CI/CD pipeline before
# deploying the new service revision.

exec "${@}"
